# Prerequisits

* Plug in dragon ISP
* Power target board

# Flashing

* `pio run` to compile
* `pio run --target program` to flash

# Notes

* If fails because wrong chip, switch chip in `platformio.ini` file
